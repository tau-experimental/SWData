
------------------------------
## 🛰️ СИСТЕМНЫЙ МАНИФЕСТ ПРОЕКТА «SWData» (v3.2)
Статус: Канонизация кодека и DDS, переход к каскаду синхронизации RX.
Железо: CH32V307 (RISC-V, RAM 64KB, жесткий запрет malloc/free, внешние 16-бит I/Q кодеки по I2S).
## 1. НАРАБОТАННОЕ API И АРХИТЕКТУРА (БОЕВЫЕ БЛОКИ)## 📌 Модуль: Сверточный кодек и Пунктуринг (conv_encoder.h/.c, puncturing.h)

* Параметры: Код NASA K=7, Rate 1/2. Полиномы: G1 = 0x6D (старший), G2 = 0x4F (младший).
* Сжатие истории Витерби (Канонический тупик): Хранение полных индексов предков требовало 53.7 КБ RAM. Было отброшено как «жестокое» для 64КБ ОЗУ.
* Удачное решение (Битовое сжатие): Поскольку топология решетки сохраняет историю сдвигов, для восстановления предка i из состояния next достаточно хранить ровно 1 выброшенный старший бит (b5).
* Боевой буфер истории: Матрица static unsigned char path_history[840][8] занимает всего 6.5 КБ RAM (сжатие в 8 раз!).
* Zero-Tail: Передатчик дописывает 6 нулей в конец пакета. Декодер начинает обратный ход (Traceback) строго из гарантированного состояния curr_state = 0, выполняя декодирование за линейное время O(1) на шаг, полностью избавившись от «квадратичного ужаса» перебора.
* Специфика багов кодека: Исправлена рассинхронизация кодера и декодера. Реальный кодер обязан обновлять регистр строго после расчета полиномов текущего бита (state7 = ((reg << 1) | bit) & 0x7F), иначе возникает фазовый сдвиг на 1 шаг, разрушающий решетку на КВ-шумах ближе к 720-му биту.

## 📌 Модуль: I/Q DDS Модулятор (modulator.h/.c)

* Раскладка частот: Дискретизация 8000 Гц, Несущая 1000 Гц, Символьная скорость 10 Бод (800 отсчетов на 1 DQPSK символ / дибит).
* Удачное решение (Точный DDS): Аккумулятор фазы unsigned short phase_acc (16 бит). Шаг фазы для 1000 Гц составляет строго 8192. Переполнение аккумулятора автоматически редуцирует угол 2π. Верхние 8 бит (phase_acc >> 8) служат индексами для 256-точечной таблицы синуса sine_lut.
* Специфика багов таблицы: Обнаружена и устранена нелинейная дисторсия (сплюснутость синуса) и клиппинг в LUT, которые порождали мощную паразитную 3-ю гармонику на 3000 Гц. Таблица инициализируется честным sin() с пиковой амплитудой 32000 под знаковый short.
* Комплексный I/Q вынос: Сигнал формируется как аналитический: $I = \cos(\phi) = \text{sine\_lut}[(idx+64)\&0xFF]$, $Q = \sin(\phi) = \text{sine\_lut}[idx]$. Зеркальный спектр аннигилирует, THD генератора < 0.001%.

------------------------------
## 2. СТРУКТУРА ЭФИРНОГО КАДРА И КВ-КАНАЛ
Сигнал на диске генерируется в комплексном формате cplx_f32 через интерфейс wav_io.c со случайной тишиной (имитация включения/выключения рации):

   1. Стартовая тишина: 0.3..0.5 сек (Истинный случайный Гауссов шум, srand(time(NULL)), SNR = 6 дБ).
   2. Частотный Пилот-тон: 1.0 сек (Чистая немодулированная комплексная несущая 1000 Гц).
   3. Синхро-код Баркера: 6 DQPSK-символов (4800 отсчетов). Маска Грея: 11, 10, 00, 10, 01, 00 (11 бит Баркера + технический ноль).
   4. Данные пакета: 420 символов (336 000 отсчетов, Rate 5/6 после пунтуринга).
   5. Финальная тишина: 0.3..0.5 сек (Шум канала).


* Модель КВ-канала связи (channel_sim.c): Добавляет AWGN шум по Боксу-Мюллеру (SNR = 6 дБ) и имитирует дрейф TCXO генератора в виде частотного сдвига ± 60 Гц (в тесте выставлено +20 Гц).

------------------------------
## 3. СТАТУС ПРИЕМНИКА (RX) И НАЙДЕННЫЕ РЕШЕНИЯ

* Грубый поиск частоты: Реализовано сверхлегкое fixed-point Radix-2 БПФ на 256 точек для MCU (вход сдвинут >> 4 для защиты от переполнения бабочек, используется целочисленное окно Хемминга). Оно успешно «взводит курок», обнаруживая пик энергии пилота в районе Bins 32-33 (шаг 31.25 Гц) посреди КВ-шума.
* Точный частотный захват (ФАПЧ): Спроектирована комплексная петля ФАПЧ 2-го порядка без тригонометрии.
* Проблема «Вялой петли»: Консервативные коэффициенты не успевали захватить +20 Гц за время пилота.
   * Решение: Интегратор частоты freq_integrator сразу инициализируется грубой подсказкой БПФ (например, стартует с +31.25 Гц, если выиграл 33-й бин). Коэффициенты петли агрессивно «разогнаны» (kp = 0.45, ki = 0.012).
   * Результат: Реактивный захват частоты за 62.5 миллисекунды (500 отсчетов). На зашумленном участке пилота петля «намертво защелкивается» на истинной частоте дрейфа.

------------------------------
## 4. ОТКРЫТЫЕ ПРОБЛЕМЫ И ТЕКУЩИЙ ПЛАН РАБОТ## 🔎 Проблема ложных срабатываний временного коррелятора Баркера

* Симптом: Коррелятор Integrate and Dump на 6 символов ложно срабатывает на тишине и пилот-тоне, а внутри пакета данных мощность пика колеблется вдвое выше эталона из-за случайных совпадений фаз данных.
* Решение на следующую сессию:
1. Внедрить динамическую нормализацию мощности коррелятора (АРУ/AGC) относительно текущей энергии в буфере, превратив абсолютный порог в относительный.
   2. Ввести стробирование: запускать коррелятор только после захвата частоты ФАПЧ, и намертво «закрывать ворота» (выключать поиск) после первого истинного пика преамбулы.

## ⏳ Проблема дрейфа тактовой частоты (Symbol Timing Drift)

* Симптом: Разбег часов TCXO передатчика и приемника даже на 1 отсчет на символ через 420 символов сместит границы интегрирования на половину длины символа, разрушая созвездие.
* Боевое решение на следующую сессию: Разработка тощей следящей петли тактовой синхронизации по переходам фазы. Так как фаза в π/4-DQPSK обязана крутиться на стыках, мы будем вычислять производную вектора и сдвигать момент сброса интегратора sample_counter на ± 1 отсчет, удерживая центр выборки в «глазу» символа без полифазных КИХ-фильтров.

## 📝 Внедрение спектрально-чистой модуляции (GMSK / CPM)

* Идея: Чтобы убрать «грязный спектр» и гармонику 3 кГц, рожденную изломами фазы, мы зафиксировали обязательное внедрение GMSK в версии v4.0. Биты данных будут предварительно сглаживаться цифровым фильтром Гаусса, превращаясь в плавные траектории плавного разгона и торможения частоты DDS (аккумулятора фазы), обеспечивая идеальную непрерывность спектра.
* Идея: для повышения надёжности зацепления за частоту удлиннить фазу пилот-тона (в процессе обсуждения).
* Идея: проверить гипотезу о том, что мощность пика корреляции станет ещё больше, если выдать два "баркера" подряд. Вопрос: существуют ли более длинны коды Баркера?

------------------------------

